{
  "name": "bjj-hub",
  "private": true,
  "scripts": {
    "web:dev": "npm --prefix apps/web run dev",
    "web:build": "npm --prefix apps/web run build",
    "web:start": "npm --prefix apps/web run start",
    "web:lint": "npm --prefix apps/web run lint",
    "web:build:ios": "npm --prefix apps/web run build:ios",
    "web:build:android": "npm --prefix apps/web run build:android",
    "web:cap:ios": "npm --prefix apps/web run cap:ios",
    "web:cap:android": "npm --prefix apps/web run cap:android"
  }
}
